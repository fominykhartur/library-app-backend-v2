export const DEFAULT_API_PORT = 9000;

export const API_PREFIX = "/library-api";

export const DEFAULT_DB_HOST = "localhost";
export const DEFAULT_DB_PORT = 3306;
export const DEFAULT_DB_CONNECTION_LIMIT = 10;
